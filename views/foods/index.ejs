<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Welcome to the Pantry</title>
</head>
<body>
    <%- include('../partials/navbar.ejs')%>
    <h1>Welcome to the Pantry</h1>
    <%foods.forEach((food) => {%>
        <ul>
            <li><%= food.name %></li>
            <form action="/users/<%= user._id %>/foods/<%= food._id %>?_method=DELETE" method="POST">
                <button type="submit">Delete Food</button>
            </form>
            <a href="/users/<%= user._id%>/foods/<%=food._id%>/edit">Edit Item</a>
        </ul>
    <%})%>
        
    <a href="/users/<%= user._id %>/foods/new">Add New Item</a>
</body>
</html>